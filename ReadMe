# **README.md**

# Application de Covoiturage – Guide d’installation & Déploiement en local

Ce document détaille la procédure complète pour installer et exécuter l’application de covoiturage **en local**, tout en utilisant **la base de données distante hébergée sur Azure**.

---

## 1. Prérequis

Avant de lancer l’application, installez les outils suivants :

### **Front-end**

* Node.js : [https://nodejs.org](https://nodejs.org)
* Angular CLI :

  ```bash
  npm install -g @angular/cli
  ```

### **Back-end**

Selon la technologie utilisée :

* PHP 8+ (XAMPP/WAMP), ou
* Node.js + Express, ou
* Java Spring Boot

*(Adapter selon ton projet)*

### Outils recommandés :

* Visual Studio Code
* Git
* Postman (pour tester l’API)

---

## 2. Base de données (hébergée sur Azure)

Votre application utilise une **base de données MySQL distante**, déjà déployée sur **Azure Database for MySQL**.
Aucune importation SQL n’est nécessaire.

### 🔐 Informations nécessaires à configurer (exemple Azure) :

* **Host** : `myc-bdd-mysql.mysql.database.azure.com`
* **Port** : `3306`
* **User** : `amarcelle@myc-bdd-mysql`
* **Password** : `aON8hSy_GeS`
* **Database** : `ecoride`

⚠️ *Ces valeurs doivent être adaptées selon votre configuration Azure.*

---

## 3. Installation du Back-End

1. Ouvrez le dossier du back-end :

   ```bash
   cd backend
   ```

2. Configurez la connexion Azure dans votre fichier de configuration :

### Exemple PHP (config.php)

```php
$host = "myc-bdd-mysql.mysql.database.azure.com";
$user = "amarcelle@myc-bdd-mysql";
$password = "aON8hSy_GeS";
$dbname = "ecoride";
```

3. Lancez votre serveur local :

* Apache (XAMPP/WAMP) pour un back-end PHP
* `npm start` pour Node.js
* `mvn spring-boot:run` pour Spring Boot

4. L’API est accessible localement (exemples) :

```
http://localhost/covoiturage-api/
http://localhost:3000/api
http://localhost:8080/api
```

---

## 4. Installation du Front-End Angular

1. Ouvrez le projet Angular :

   ```bash
   cd front
   ```

2. Installez les dépendances :

   ```bash
   npm install
   ```

3. Configurez l’URL de l’API dans `src/environments/environment.ts` :

   ```ts
   export const environment = {
     apiUrl: 'http://localhost:3000/api'  // ou Apache, ou Spring Boot
   };
   ```

4. Lancez Angular :

   ```bash
   ng serve
   ```

5. Ouvrez l’application :

```
http://localhost:4200/
```

---

## 5. Tests

### API

Utiliser Postman pour tester :

* Connexion utilisateur
* Création de covoiturage
* Participation
* Ajout d’avis

### Front

Vérifier :

* Formulaires
* Filtrage des trajets
* Gestion du profil
* Réservation

---

## 6. Problèmes courants & solutions

| Problème              | Solution                                              |
| --------------------- | ----------------------------------------------------- |
| Erreur SSL Azure      | Activer "SSL required = true" dans la connexion MySQL |
| ECACCESS DENIED Azure | Vérifier firewall Azure (ajouter son IP)              |
| Angular → API : CORS  | Activer CORS dans le back-end                         |
| Temps de réponse lent | Optimiser les requêtes MySQL                          |

---

## 7. Comptes utilisateurs de test

| Rôle        | Email             | Mot de passe  |
| ----------- | ----------------- | ------------- |
| Admin       | [admin@sfr.fr]    | Admin123!     |
| Utilisateur | [test@test.com]   | user123       |

---

## 8. Commandes utiles

### Général

```bash
git clone <url>
npm install
ng serve
```

### API Node.js

```bash
npm start
```

---

## 9. Informations

Projet académique 2024–2025
Développé par : **MARCELLE Allan**


